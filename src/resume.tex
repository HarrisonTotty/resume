$[ do require('short_name', 'this.sections') ]$

$[ include 'style.template' ]$

$[ include 'info.template' ]$

%-------------------------------------------------------------------------------
\begin{document}

% Prints the header with alignment Left (L), Center (C), or Right (R).
\makecvheader[C]

% Prints the footer.
\makecvfooter
  {\today}
  {[[ short_name ]]~~~·~~~Résumé}
  {\thepage}

% ----- Sections -----
$[ for section in this.sections ]$
\input{sections/[[ section ]].tex}
$[ endfor ]$

\end{document}
%-------------------------------------------------------------------------------
